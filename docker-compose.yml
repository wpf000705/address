services:
  eth-address-generator:
    build: .
    container_name: eth-address-generator
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
